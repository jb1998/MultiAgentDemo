[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    unit: fast isolated tests (no HTTP, minimal I/O)
    integration: multi-module tests with database
    api: HTTP API contract tests via TestClient
    e2e: full user journeys through the API
